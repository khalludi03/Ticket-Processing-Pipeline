{
  "name": "ticket-pipeline",
  "private": true,
  "scripts": {
    "generate": "bunx drizzle-kit generate",
    "migrate": "bun run src/db/migrate.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9",
    "zod": "^4.3.6"
  }
}
